Calculator program capable of operations with Fractons and/or Mixed Fractions in GUI implementation.

# Limitations
- This calculator only has the basic operations, with a fraction and a mixed number as extra features.
- It only calculates between two operands, but it can have the previous result as an operand to the next operation.
